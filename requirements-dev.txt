# Development + test dependencies.
-r requirements.txt
pytest>=8.0,<10.0
httpx>=0.27,<1.0
ruff>=0.6,<1.0
# only needed to regenerate the synthetic sample data (scripts/generate_sample_data.py)
reportlab>=4.0,<6.0
python-docx>=1.1,<2.0
